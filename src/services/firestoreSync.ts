/**
 * SERVIÇO DE PERSISTÊNCIA NATIVO DO SERVIDOR (OFFLINE-FIRST & REAL-TIME)
 * 100% livre de cadastros externos, tolerante a internet instável e falhas de Wi-Fi.
 */

export * from './nativeDatabaseSync';
export { default } from './nativeDatabaseSync';
