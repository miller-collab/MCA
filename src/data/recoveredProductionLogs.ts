import { ProductionLog } from '../types';

/**
 * Empty by default - DO NOT inject simulated or mock logs.
 * Real logs must only be recorded by actual factory operators and stored securely.
 */
export const ALL_RECOVERED_LOGS: ProductionLog[] = [];
